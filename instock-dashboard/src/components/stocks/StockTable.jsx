import { Eye, Pencil, Trash2 } from 'lucide-react';
const labels = { in_stock: 'En stock', low_stock: 'Stock faible', critical_stock: 'Stock critique', out_of_stock: 'Rupture de stock' };
function badge(status) { return <span className={`stock-status status-${status}`}>{labels[status]}</span>; }
export default function StockTable({ products, loading, onView, onEdit, onDelete }) { if (loading) return <div className="stock-skeleton">{Array.from({ length: 6 }, (_, index) => <div key={index} />)}</div>; return <div className="stock-table-wrap"><table className="stock-table"><thead><tr><th>Produit</th><th>Référence</th><th>Catégorie</th><th>Stock disponible</th><th>Seuil minimum</th><th>Emplacement</th><th>Statut</th><th>Actions</th></tr></thead><tbody>{products.map((product) => <tr key={product.id}><td><div className="stock-product"><img src={product.image} alt="" /><strong>{product.name}</strong></div></td><td>{product.reference}</td><td>{product.category}</td><td><strong>{product.quantity}</strong></td><td>{product.minThreshold}</td><td>{product.location}</td><td>{badge(product.status)}</td><td><div className="stock-actions"><button type="button" onClick={() => onView(product)} aria-label={`Voir ${product.name}`}><Eye size={15} /></button><button type="button" onClick={() => onEdit(product)} aria-label={`Modifier ${product.name}`}><Pencil size={15} /></button><button type="button" onClick={() => onDelete(product)} aria-label={`Supprimer ${product.name}`}><Trash2 size={15} /></button></div></td></tr>)}</tbody></table></div>; }
export { labels };
