import { Trash2, X } from 'lucide-react';
export default function StockDeleteConfirm({ open, onClose, product, onConfirm }) { if (!open || !product) return null; return <div className="stock-modal-backdrop" onMouseDown={(event) => event.target === event.currentTarget && onClose()}><section className="stock-modal delete-modal" role="dialog" aria-modal="true" aria-labelledby="delete-stock-title"><button type="button" className="delete-close" onClick={onClose} aria-label="Fermer"><X size={18}/></button><span className="delete-icon"><Trash2 size={22}/></span><h2 id="delete-stock-title">Supprimer ce produit ?</h2><p>« {product.name} » sera retiré des données de démonstration.</p><footer><button type="button" className="stock-secondary" onClick={onClose}>Annuler</button><button type="button" className="delete-button" onClick={() => { onConfirm(product); onClose(); }}>Supprimer</button></footer></section></div>; }
