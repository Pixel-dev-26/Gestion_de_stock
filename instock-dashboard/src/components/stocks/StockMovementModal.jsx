import { X } from 'lucide-react';
import { useState } from 'react';
export default function StockMovementModal({ open, onClose, products, onSubmit }) { const [productId, setProductId] = useState(products[0]?.id || ''); if (!open) return null; function submit(event) { event.preventDefault(); const form = new FormData(event.currentTarget); onSubmit({ id: Date.now(), productId: Number(productId), type: form.get('type'), quantity: Number(form.get('quantity')), date: form.get('date'), reason: form.get('reason'), observation: form.get('observation') }); onClose(); } return <div className="stock-modal-backdrop" onMouseDown={(event) => event.target === event.currentTarget && onClose()}><section className="stock-modal" role="dialog" aria-modal="true" aria-labelledby="movement-title"><header><div><span>Mode démonstration</span><h2 id="movement-title">Ajouter un mouvement</h2></div><button type="button" onClick={onClose} aria-label="Fermer"><X size={19}/></button></header><form onSubmit={submit}><label>Produit<select value={productId} onChange={(event) => setProductId(event.target.value)}>{products.map((product) => <option key={product.id} value={product.id}>{product.name} · {product.reference}</option>)}</select></label><div className="stock-form-grid"><label>Type<select name="type"><option value="in">Entrée</option><option value="out">Sortie</option></select></label><label>Quantité<input name="quantity" type="number" min="1" defaultValue="1" required /></label></div><label>Date<input name="date" type="date" defaultValue={new Date().toISOString().slice(0, 10)} required /></label><label>Motif<input name="reason" placeholder="Réception, commande, inventaire..." required /></label><label>Observation<textarea name="observation" rows="3" placeholder="Note facultative" /></label><footer><button type="button" className="stock-secondary" onClick={onClose}>Annuler</button><button type="submit" className="stock-primary">Enregistrer</button></footer></form></section></div>; }
