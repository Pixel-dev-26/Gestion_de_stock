import { Eye, Pencil, Trash2 } from 'lucide-react';
import { labels } from './StockTable.jsx';
export default function StockCard({ products, loading, onView, onEdit, onDelete }) { if (loading) return <div className="stock-card-list">{Array.from({ length: 4 }, (_, index) => <div className="stock-card skeleton-card" key={index} />)}</div>; return <div className="stock-card-list">{products.map((product) => <article className="stock-card" key={product.id}><div className="stock-card-head"><img src={product.image} alt="" /><div><h3>{product.name}</h3><p>{product.reference}</p></div><span className={`stock-status status-${product.status}`}>{labels[product.status]}</span></div><div className="stock-card-grid"><span><small>Catégorie</small>{product.category}</span><span><small>Stock</small>{product.quantity} unités</span><span><small>Seuil</small>{product.minThreshold}</span><span><small>Emplacement</small>{product.location}</span></div><div className="stock-actions"><button type="button" onClick={() => onView(product)}><Eye size={15} /> Voir</button><button type="button" onClick={() => onEdit(product)}><Pencil size={15} /> Modifier</button><button type="button" onClick={() => onDelete(product)} aria-label="Supprimer"><Trash2 size={15} /></button></div></article>)}</div>; }
