import { X } from 'lucide-react';
export default function StockEditModal({ open, onClose, product, onSubmit }) { if (!open || !product) return null; function submit(event) { event.preventDefault(); const form = new FormData(event.currentTarget); onSubmit({ ...product, quantity: Number(form.get('quantity')), minThreshold: Number(form.get('minThreshold')), location: form.get('location') }); onClose(); } return <div className="stock-modal-backdrop" onMouseDown={(event) => event.target === event.currentTarget && onClose()}><section className="stock-modal" role="dialog" aria-modal="true" aria-labelledby="edit-stock-title"><header><div><span>Mode démonstration</span><h2 id="edit-stock-title">Modifier le stock</h2></div><button type="button" onClick={onClose} aria-label="Fermer"><X size={19}/></button></header><form onSubmit={submit}><p className="modal-product-name">{product.name} · {product.reference}</p><div className="stock-form-grid"><label>Quantité<input name="quantity" type="number" min="0" defaultValue={product.quantity} required /></label><label>Seuil minimum<input name="minThreshold" type="number" min="0" defaultValue={product.minThreshold} required /></label></div><label>Emplacement<input name="location" defaultValue={product.location} required /></label><footer><button type="button" className="stock-secondary" onClick={onClose}>Annuler</button><button type="submit" className="stock-primary">Enregistrer</button></footer></form></section></div>; }
