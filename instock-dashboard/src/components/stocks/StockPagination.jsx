import { ChevronLeft, ChevronRight } from 'lucide-react';
export default function StockPagination({ currentPage, totalPages, onPageChange, disabled }) { return <nav className="stock-pagination" aria-label="Pagination des stocks"><button type="button" disabled={disabled || currentPage === 1} onClick={() => onPageChange(currentPage - 1)} aria-label="Page précédente"><ChevronLeft size={16} /></button><span>Page {currentPage} / {totalPages}</span><button type="button" disabled={disabled || currentPage === totalPages} onClick={() => onPageChange(currentPage + 1)} aria-label="Page suivante"><ChevronRight size={16} /></button></nav>; }
