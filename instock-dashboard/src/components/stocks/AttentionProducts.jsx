import { AlertTriangle, ArrowRight } from 'lucide-react';
import { labels } from './StockTable.jsx';
export default function AttentionProducts({ products, onView }) { const attention = products.filter((product) => product.status !== 'in_stock'); return <section className="attention-panel"><header><span className="attention-icon"><AlertTriangle size={17} /></span><div><h2>Produits nécessitant une attention</h2><p>Stock faible, critique ou en rupture</p></div></header><div className="attention-list">{attention.map((product) => <button type="button" key={product.id} onClick={() => onView(product)}><span><strong>{product.name}</strong><small>{product.reference} · {product.quantity} / {product.minThreshold} unités</small></span><span className={`stock-status status-${product.status}`}>{labels[product.status]}</span><ArrowRight size={15} /></button>)}</div></section>; }
