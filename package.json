{
  "name": "gestion-de-stock-root",
  "private": true,
  "workspaces": [
    "instock-dashboard"
  ],
  "scripts": {
    "build": "npm run build --workspace=instock-dashboard",
    "vercel-build": "npm run build --workspace=instock-dashboard"
  }
}
